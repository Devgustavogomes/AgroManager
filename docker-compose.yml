services:
  server:
    image: node:22.slim
    build:
      context: ./backendnest
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    env_file:
      - .env
    container_name: skillhub-backend-prod
